name: streamy # you probably want to 'snapcraft register <name>'
version: '0.9' # just for humans, typically '1.2+git' or '1.3.2'
summary: Torrent to HTTP proxy # 79 char long summary
description: |
  Downald things from DHT like it was HTTP server.

grade: stable # must be 'stable' to release into candidate/stable channels
confinement: strict # use 'strict' once you have the right plugs and slots

parts:
  my-part:
    # See 'snapcraft plugins'
    plugin: nil
